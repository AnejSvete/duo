train: formal
valid: formal
tokenizer_name_or_path: formal
cache_dir: /cluster/scratch/asvete/duo
wrap: False
streaming: False
insert_train_eos: False
insert_valid_eos: False
formal:
  num_examples_train: 10000000
  num_examples_valid: 10000
  num_vars: 5
  max_depth: 5      # Max depth for the formula tree; each formula's depth is chosen from 1..max_depth.
  max_fan_in: 5     # Max number of children for intermediate AND/OR nodes.
  format: full_trace # Options: full_trace, final_value