train: bfvp
valid: bfvp
tokenizer_name_or_path: formal
cache_dir: /cluster/scratch/asvete/duo
wrap: False
streaming: False
insert_train_eos: False
insert_valid_eos: False
properties:
  num_examples_train: 1000000
  num_examples_valid: 10000
  num_vars: 3
  max_depth: 4      # Max depth for the formula tree; each formula's depth is chosen from 1..max_depth.
  fan_in: 3         # Fixed fan-in.
  format: trace     # Options: trace, final_value