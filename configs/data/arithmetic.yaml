defaults:
  - formal_language

language: arithmetic
properties:
  min_val: 1
  max_val: 50

  # Depth ranges for train/val/test splits
  # Depth controls expression complexity and correlates with sequence length
  # Larger depth values create deeper expression trees with more operations
  min_train_depth: 1
  max_train_depth: 4
  min_val_depth: 3
  max_val_depth: 5
  min_test_depth: 4
  max_test_depth: 6