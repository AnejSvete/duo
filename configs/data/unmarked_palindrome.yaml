defaults:
  - formal_language

language: unmarked_palindrome
properties:
  alphabet: "a,b"  # Comma-separated alphabet symbols

  # Length ranges for generation and stratification
  # Length represents total number of symbols in the palindrome
  min_train_len: 2
  max_train_len: 32
  min_val_len: 16
  max_val_len: 32
  min_test_len: 24
  max_test_len: 32
